import { TitleStringifyPipe } from './title-stringify.pipe';

describe('TitleStringifyPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleStringifyPipe();
    expect(pipe).toBeTruthy();
  });
});
